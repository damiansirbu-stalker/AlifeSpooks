--- _as_manifest: AlifeSpooks identity. Pure data, no logic. Read by _as_init (the gate) and the validator (deps.manifest_structure).

MANIFEST = {
    name    = "AlifeSpooks",
    version = "1.0.0-snapshot",
    xlibs   = "1.8.5",
}
